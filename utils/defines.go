package utils

const NONE_GROUP = "none"

const WIZARD_EXT = ".wizard"

const COMMANDS_MSG = `Commands:
    list, -l        list all wizards
    <wizard>        run wizard
    <group> <wizard>    run wizard in group
    config, --config    print the current configuration
    version, --version  print the version
    help, -h, --help    print this help message
`
